using System;
using System.Collections.Generic;
using System.Text;

namespace EntityDock.Lib.Base
{
    /*public sealed class IndexAttibute : SchemaAttibute
    {
        public IndexAttibute(string prop, bool isUnique = false)
        {
            if (string.IsNullOrEmpty(prop))
            {
                throw new ArgumentException($"'{nameof(prop)}' cannot be null or empty.", nameof(prop));
            }
            Prop = prop;
            IsUnique = isUnique;
        }

        public string Prop { get; }
        public bool IsUnique { get; }
    }*/
}
